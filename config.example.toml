# Merge only the settings you want into your Codex configuration. The optional
# lead defaults are shown explicitly. Keep an existing explicit model or effort.
#
# model = "gpt-6-astra"
# model_reasoning_effort = "high"

[agents]
default_subagent_model = "gpt-6-sol"
default_subagent_reasoning_effort = "high"
max_concurrent_threads_per_session = 3

[project]
name = "codex-development-harness"
version = "0.1.0"
description = "Portable helpers for bounded Codex task packets and evidence checks"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

